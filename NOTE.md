Oui, voilà exactement quoi noter. Je distingue bien **ce qui vient directement des tutos** et **ce qui est une adaptation à ton projet CSV**.

| Élément du projet                                  | Où c’est dans les tutos ?                                                                                                                                    | Ce qu’on a fait                                                                                 |
| -------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------- |
| Projet Android en **Java**, pas Kotlin             | Le tuto première application dit qu’on travaillera toujours en Java.                                                                                         | Tous les fichiers Android sont en Java.                                                         |
| `MainActivity extends AppCompatActivity`           | Le tuto montre la structure minimale de `MainActivity` avec `extends AppCompatActivity`.                                                                     | On garde cette structure.                                                                       |
| `setContentView(R.layout.activity_main)`           | Le tuto montre que `setContentView(...)` charge le fichier XML de l’interface.                                                                               | On charge `activity_main.xml`.                                                                  |
| `findViewById(...)`                                | Le tuto explique que le lien entre XML et Java se fait avec les identifiants et `findViewById()`.                                                            | On récupère `TextView`, `ProgressBar`, `ListView`, boutons, etc.                                |
| Bouton avec `android:onClick="reagir"`             | Le tuto montre l’attribut `onClick` et une méthode `public void reagir(View view)`.                                                                          | Les boutons `RT1FI`, `RT1FA`, etc. appellent `reagir`.                                          |
| Pas de lambda, pas de classe anonyme               | Le tuto dit explicitement : “On n’utilisera jamais de lambdas dans nos codes, pas plus que de classe anonyme.”                                               | On utilise des classes normales : `MonThread`, `UpdateUITask`, `TaskListener`.                  |
| Permission Internet                                | Le tuto première application et le tuto réseau disent d’ajouter `<uses-permission android:name="android.permission.INTERNET" />` avant `<application>`.      | On l’ajoute dans `AndroidManifest.xml`.                                                         |
| Socket Android vers serveur                        | Le tuto montre l’utilisation de `Socket` et explique que `10.0.2.2` correspond au localhost du PC hôte depuis l’émulateur.                                   | On utilise `10.0.2.2` pour joindre ton serveur WSL.                                             |
| Ne pas faire le réseau directement sur l’UI thread | Le tuto explique que les opérations réseau/longues doivent être déléguées à un thread secondaire.                                                            | On ne met pas la socket directement dans `MainActivity`, on passe par `MonThread`.              |
| `TaskListener`                                     | Le tuto multithreading crée une interface `TaskListener` avec `onTaskComplete(String result)`.                                                               | On utilise pareil pour prévenir `MainActivity` quand la récupération est finie.                 |
| `MonThread extends Thread`                         | Le tuto montre une classe `MonThread` qui hérite de `Thread`, reçoit un `TaskListener`, implémente `run()`, puis appelle `listener.onTaskComplete(result)`.  | Notre `MonThread` récupère le CSV au lieu de simuler avec `sleep`.                              |
| `thread.start()` et pas `run()`                    | Le tuto dit clairement qu’on appelle `start()` et jamais directement `run()`.                                                                                | Dans `MainActivity`, on fait `thread.start()`.                                                  |
| `UpdateUITask implements Runnable`                 | Le tuto crée une classe `UpdateUITask` qui implémente `Runnable` pour modifier l’interface.                                                                  | On a une classe `UpdateUITask` qui met à jour la `ListView`, le `TextView` et la `ProgressBar`. |
| `runOnUiThread(updateUITask)`                      | Le tuto explique que seul le thread principal peut modifier l’interface et utilise `runOnUiThread(...)`.                                                     | Dans `onTaskComplete`, on appelle `runOnUiThread(updateUITask)`.                                |
| `ProgressBar`                                      | Le tuto multithreading ajoute une `ProgressBar`, visible pendant le traitement puis masquée après.                                                           | On l’affiche pendant le chargement du CSV, puis on la cache.                                    |
| `ListView`                                         | Le tuto ListView explique d’ajouter un widget `ListView` dans le XML et de le récupérer avec `findViewById`.                                                 | On affiche les étudiants dans une `ListView`.                                                   |
| `ArrayAdapter<String>`                             | Le tuto ListView utilise un `ArrayAdapter<String>` pour faire le lien entre les données et la `ListView`.                                                    | On lie `ArrayList<String> lignesCSV` à la `ListView`.                                           |
| `ArrayList<String>`                                | Le tuto ListView remplace le tableau `String[]` par un `ArrayList<String>` pour rendre la liste modifiable.                                                  | On stocke les étudiants dans `ArrayList<String> lignesCSV`.                                     |
| `adapter.notifyDataSetChanged()`                   | Le tuto ListView dit qu’après modification de la liste, il faut prévenir l’adapter avec `notifyDataSetChanged()`.                                            | Après réception du CSV, on met à jour la liste puis on appelle `notifyDataSetChanged()`.        |
| Cases à cocher dans la `ListView`                  | Le tuto ListViewWithChecks remplace `simple_list_item_1` par `simple_list_item_multiple_choice` et utilise `setChoiceMode(ListView.CHOICE_MODE_MULTIPLE)`.   | On affiche les étudiants avec des cases à cocher pour la présence.                              |
| `ArrayList<Boolean> presents`                      | Le tuto ListViewWithChecks ajoute `private ArrayList<Boolean> presents;` pour relier les cases à des données.                                                | On stocke `true` si présent, `false` si absent.                                                 |
| Présents par défaut                                | Le tuto initialise toutes les présences à `true` avec une boucle.                                                                                            | Tous les étudiants sont cochés au départ.                                                       |
| `listView.setItemChecked(i, presents.get(i))`      | Le tuto utilise cette méthode pour que l’affichage corresponde au tableau `presents`.                                                                        | On coche les lignes selon leur valeur dans `presents`.                                          |
| `implements AdapterView.OnItemClickListener`       | Le tuto ListViewWithChecks utilise cet écouteur pour gérer le clic sur une ligne.                                                                            | `MainActivity` implémente `AdapterView.OnItemClickListener`.                                    |
| `onItemClick(...)`                                 | Le tuto récupère `isChecked`, met à jour `presents.set(position, isChecked)` et affiche présent/absent.                                                      | Quand tu coches/décoches, on met à jour la présence.                                            |
| `System.out.printf(...)`                           | Le tuto utilise `System.out.printf("L'étudiant %s est %s\n", nom, status);` pour vérifier dans Logcat.                                                       | On garde cette trace dans Logcat.                                                               |

Les seules choses qui sont **adaptées à ton projet**, donc pas écrites mot pour mot dans les tutos, sont :

| Adaptation                                 | Pourquoi c’est quand même logique                                                                                                                                                                    |
| ------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Découper le CSV avec `split(";")`          | Les tutos parlent d’afficher une liste d’étudiants, mais ton serveur renvoie du CSV. Il faut donc convertir chaque ligne CSV en texte affichable.                                                    |
| Afficher `nom prenom (TP)`                 | Les tutos utilisent `Pierre`, `Paul`, `Jacques` comme exemples. Nous, on adapte l’affichage à tes colonnes CSV.                                                                                      |
| Boutons `RT1FI`, `RT1FA`, `RT2FI`, `RT2FA` | Le tuto montre des boutons avec `onClick`. Les noms des groupes viennent de ton projet SAE.                                                                                                          |
| `LectureClasse.java` côté Android et CLI   | Le tuto demande un client texte et un client Android qui demandent une liste au serveur, et précise de ne pas faire de redondance de code.  Donc une classe commune de lecture réseau est cohérente. |
| Version CLI avec présence `y/n`            | Les tutos Android utilisent des checkbox. En console, il n’y a pas de checkbox, donc `y/n` est juste l’équivalent texte de coché/décoché.                                                            |

Donc pour ton compte rendu, tu peux écrire :

> L’interface Android reprend les principes des tutoriels : `findViewById`, `onClick`, `ListView`, `ArrayAdapter`, `ArrayList`, `notifyDataSetChanged`, `TaskListener`, `Thread`, `Runnable`, `runOnUiThread` et `ListView` avec cases à cocher. Les seules adaptations concernent le format CSV reçu du serveur et l’affichage sous forme `nom prenom (TP)`.
